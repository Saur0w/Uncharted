
import ServicesClient from './ServicesClient';

export const metadata = {
    title: 'Services | Payal Joshi & Associates',
    description: 'Our services include indirect-taxes planning & filing, business registration, audit & assurance, GST services, and more.',
}

export default function ServicesPage() {
    return <ServicesClient />
}